# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
*.xcworkspace/xcuserdata/

# XcodeGen 생성물 — project.yml이 SSOT. 클론 후 `xcodegen generate`로 재생성.
*.xcodeproj/
App/Info.plist

# Swift Package Manager
.build/
.swiftpm/

# 빌드 산출물
build/
dist/

# 비밀정보 — 키움 app key/secret/token 은 절대 커밋 금지 (Keychain 사용)
.env
.env.*
Secrets.plist
secrets.json
*.token
